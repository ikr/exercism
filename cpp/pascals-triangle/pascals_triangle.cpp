#include "pascals_triangle.h"

namespace {
using Vec = std::vector<int>;
using VVec = std::vector<Vec>;
} // namespace

namespace pascals_triangle {
VVec generate_rows(unsigned int last_row_index) {
    VVec ans(last_row_index);
    return ans;
}
} // namespace pascals_triangle
