#if !defined(CLOCK_H)
#define CLOCK_H

#include <string>

namespace date_independent {
namespace clock {
std::string at(const int hour, const int minute);
} // namespace clock
} // namespace date_independent

#endif // CLOCK_H
